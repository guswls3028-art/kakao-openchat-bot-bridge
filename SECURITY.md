# Security

## Secrets

Never commit these values:

- `KAKAO_BOT_SECRET`
- `QUESTION_API_SECRET`
- real chat exports or event logs
- deployment SSH keys or server IPs if you do not intend to disclose them

## Reporting

Open a GitHub issue for general hardening suggestions. If a report includes private tokens, logs, or room information, remove the sensitive parts before posting.
